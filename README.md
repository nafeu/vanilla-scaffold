# Vanilla Scaffold

Fast github pages boilerplate generator for a plain HTML/CSS/JS websites

### Installation

```
npm install
npm link
cp .sample-env .env
```

Fill out the `.env` file

### Basic Usage

```
vscaf
```
